# HTTP server port
server.port=8080

# S3 bucket that stores uploaded files
app.aws.s3.bucket-name=your-upload-bucket

# AWS region; if omitted the AWS SDK default chain is used
app.aws.region=us-east-1
